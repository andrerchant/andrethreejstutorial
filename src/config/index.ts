export const CAMERA_CF = {
    fov: 45,
    aspect: window.innerWidth / window.innerHeight,
    near: 0.1,
    far: 1000,
};

export const AXES_LENGTH = 4;
export const cameraInital = {
    x: -10,
    z: 30,
    y: 30,
};
